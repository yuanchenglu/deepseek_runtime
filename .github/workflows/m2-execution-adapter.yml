name: M2 ExecutionAdapter Gate

on:
  push:
    branches: [develop]
  pull_request:
    branches: [develop, master]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: m2-execution-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  execution:
    name: ${{ matrix.os }} / Python 3.11
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    timeout-minutes: 20

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      - name: Install package
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e .

      - name: Run M2-C focused gate
        run: >-
          python scripts/m2_execution_gate.py
          --output "m2-execution-${{ runner.os }}.json"

      - name: Upload M2-C evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: m2-execution-${{ runner.os }}
          path: m2-execution-${{ runner.os }}.json
          if-no-files-found: error
          retention-days: 30
