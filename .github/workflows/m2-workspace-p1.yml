name: M2 Workspace P1 Gate

on:
  pull_request:
    branches: [develop, master]
  push:
    branches: [develop]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: m2-workspace-p1-${{ github.workflow }}-${{ github.ref }}-${{ github.sha }}
  cancel-in-progress: false

jobs:
  workspace:
    name: ${{ matrix.os }} / Python 3.11
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    timeout-minutes: 20

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      - name: Install package
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e .

      - name: Run M2-E focused gate
        run: >-
          python scripts/m2_workspace_gate.py
          --output "m2-workspace-p1-${{ runner.os }}.json"

      - name: Upload M2-E evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: m2-workspace-p1-${{ runner.os }}
          path: m2-workspace-p1-${{ runner.os }}.json
          if-no-files-found: error
          retention-days: 30
